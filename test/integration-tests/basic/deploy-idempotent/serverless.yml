# Deploying should be idempotent
service: sls-domain-manager-deploy-idempotent
provider:
  lambdaHashingVersion: 20201221
  name: aws
  runtime: nodejs12.x
  region: us-west-2
  stage: test
functions:
  helloWorld:
    handler: handler.helloWorld
    events:
      - http:
          path: hello-world
          method: get
          cors: true
plugins:
  - serverless-domain-manager
custom:
  customDomain:
    domainName: sls-domain-manager-deploy-idempotent.${env:TEST_DOMAIN}
    basePath: ''

package:
  exclude:
    - node_modules/**
