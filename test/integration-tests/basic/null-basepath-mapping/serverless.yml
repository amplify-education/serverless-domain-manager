# Enabled and testing basepath mapping
service: sls-domain-manager-null-basepath-mapping
provider:
  lambdaHashingVersion: 20201221
  name: aws
  runtime: nodejs12.x
  region: us-west-2
  stage: test
functions:
  helloWorld:
    handler: handler.helloWorld
    events:
      - http:
          path: hello-world
          method: get
          cors: true
plugins:
  - serverless-domain-manager
custom:
  customDomain:
    domainName: sls-domain-manager-null-basepath-mapping.${env:TEST_DOMAIN}
    basePath: ''

package:
  exclude:
    - node_modules/**
