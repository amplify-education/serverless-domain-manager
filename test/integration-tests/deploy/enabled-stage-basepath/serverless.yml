# Enabled with custom stage and empty basepath
service: sls-dm-enabled-stage-basepath
provider:
  lambdaHashingVersion: 20201221
  name: aws
  runtime: nodejs12.x
  region: us-west-2
  stage: test
functions:
  helloWorld:
    handler: handler.helloWorld
    events:
      - http:
          path: hello-world
          method: get
          cors: true
plugins:
  - serverless-domain-manager
custom:
  customDomain:
    domainName: sls-dm-enabled-stage-basepath.${env:TEST_DOMAIN}
    stage: test
    basePath: ''

package:
  exclude:
    - node_modules/**
