# Test regional domains with TLS 1.0
service: sls-dm-regional-tls-1-0
provider:
  lambdaHashingVersion: 20201221
  name: aws
  runtime: nodejs12.x
  region: us-west-2
  stage: test
  endpointType: regional

functions:
  helloWorld:
    handler: handler.helloWorld
    events:
      - http:
          path: hello-world
          method: get
          cors: true
plugins:
  - serverless-domain-manager
custom:
  customDomain:
    domainName: sls-dm-regional-tls-1-0.${env:TEST_DOMAIN}
    securityPolicy: tls_1_0
    endpointType: regional

package:
  exclude:
    - node_modules/**
