service: sls-dm-route-53-latency-routing
provider:
  lambdaHashingVersion: 20201221
  name: aws
  runtime: nodejs12.x
  region: us-west-2
  endpointType: regional
  stage: test
functions:
  helloWorld:
    handler: handler.helloWorld
    events:
      - http:
          path: hello-world
          method: get
          cors: true
plugins:
  - serverless-domain-manager
custom:
  customDomain:
    domainName: sls-dm-route-53-latency-routing.${env:TEST_DOMAIN}
    endpointType: regional
    route53Params:
      routingPolicy: latency

package:
  exclude:
    - node_modules/**
