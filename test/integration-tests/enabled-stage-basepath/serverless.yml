# Enabled with custom stage and empty basepath
service: enabled-stage-basepath
provider:
  name: aws
  runtime: nodejs10.x
  region: us-east-1
  stage: test
functions:
  helloWorld:
    handler: handler.helloWorld
    events:
      - http:
          path: hello-world
          method: get
          cors: true
package:
  exclude:
    - node_modules/**
plugins:
  - serverless-domain-manager
custom:
  customDomain:
    - domainName: enabled-stage-basepath-rest-${opt:RANDOM_STRING}.${env:TEST_DOMAIN}
      stage: test
      basePath: ''
    - domainName: enabled-stage-basepath-websocket-${opt:RANDOM_STRING}.${env:TEST_DOMAIN}
      stage: test
      basePath: ''
      websocket: true
