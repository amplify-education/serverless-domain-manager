# TEST 2
# Serverless Domain Manager
service: serverless-hello-world-test2
provider:
  name: aws
  runtime: nodejs6.10
  region: us-west-2
functions:
  helloWorld:
    handler: handler.helloWorld
    events:
      - http:
          path: hello-world
          method: get
          cors: true
plugins:
  - serverless-domain-manager
custom:
  customDomain:
    domainName: test2.${env:TEST_DOMAIN}
    # stage: dev
    basePath: 'api'
    # endpointType: regional
    # certificateName: ''
    # certificateArn: "(none)"
    # createRoute53Record: true
    # enabled: true
